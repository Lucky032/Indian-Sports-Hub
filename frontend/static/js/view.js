document.getElementById('Next').addEventListener('click', function() {
    // Call a function to fetch the next profile data
    fetchNextProfile();
});

document.getElementById('Previous').addEventListener('click', function() {
    // Call a function to fetch the previous profile data
    fetchPrevProfile();
});
